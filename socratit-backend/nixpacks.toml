[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
    "npm ci --ignore-scripts"
]

[phases.build]
cmds = [
    "npm run build"
]

[start]
cmd = "./node_modules/.bin/prisma migrate deploy && npm run start"
