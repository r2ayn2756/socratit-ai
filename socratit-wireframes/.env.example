# Frontend Environment Variables

# API Configuration
REACT_APP_API_URL=http://localhost:3001

# WebSocket Configuration (uses same URL as API)
# WebSocket connection is established automatically at the same URL
